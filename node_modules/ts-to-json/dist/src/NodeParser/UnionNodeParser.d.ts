import ts from "typescript";
import { Context, NodeParser } from "../NodeParser";
import { SubNodeParser } from "../SubNodeParser";
import { BaseType } from "../Type/BaseType";
import { Config } from "../Config";
export declare class UnionNodeParser implements SubNodeParser {
    protected typeChecker: ts.TypeChecker;
    protected childNodeParser: NodeParser;
    private config;
    constructor(typeChecker: ts.TypeChecker, childNodeParser: NodeParser, config: Config);
    supportsNode(node: ts.UnionTypeNode): boolean;
    createType(node: ts.UnionTypeNode, context: Context): BaseType | undefined;
}
