"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasLimitOptions = void 0;
const hasLimitOptions = (config) => {
    if (!config)
        return;
    if (config.excludeRootProps && config.excludeRootProps.length)
        return true;
    if (config.excludeProps && config.excludeProps.length)
        return true;
    if (config.includeProps && config.includeProps.length)
        return true;
    if (config.maxDepth)
        return true;
    if (config.funcParamMaxDepth)
        return true;
    return false;
};
exports.hasLimitOptions = hasLimitOptions;
//# sourceMappingURL=hasLimitOptions.js.map