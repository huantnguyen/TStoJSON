"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.shouldParseNode = void 0;
const _1 = require(".");
const shouldParseNode = (node, config) => {
    const { shouldParseNode: shouldParseNodeProp, skipParseFiles } = config;
    if ((0, _1.isInForceParseTypes)(node, config))
        return true;
    if (shouldParseNodeProp) {
        if (!shouldParseNodeProp(node))
            return false;
    }
    if (skipParseFiles) {
        if (skipParseFiles.find((x) => node.getSourceFile().fileName.includes(x))) {
            return false;
        }
    }
    return true;
};
exports.shouldParseNode = shouldParseNode;
//# sourceMappingURL=isNodeSkipped.js.map