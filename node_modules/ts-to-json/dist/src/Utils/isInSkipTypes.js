"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isInSkipTypes = void 0;
const node_name_1 = require("./node-name");
const isFunctionKind_1 = require("./isFunctionKind");
const isInSkipTypes = (node, config) => {
    if (!config.skipParseTypes || !config.skipParseTypes.length)
        return false;
    const name = (0, node_name_1.getNodeName)(node);
    if (name) {
        if (config.skipParseTypes.includes(name))
            return true;
    }
    if (config.skipParseTypes.includes("function") && (0, isFunctionKind_1.isFunctionKind)(node)) {
        return true;
    }
    return false;
};
exports.isInSkipTypes = isInSkipTypes;
//# sourceMappingURL=isInSkipTypes.js.map