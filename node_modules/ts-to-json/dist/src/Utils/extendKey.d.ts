import { Context } from "../NodeParser";
import * as ts from "typescript";
import { Config } from "../Config";
export declare const shouldExtendKey: (context: Context, config: Config) => boolean;
export declare const extendKey: (key: string, node: ts.Node, context: Context, config: Config, ignoredIfSameAsConfigTypeName?: boolean | undefined) => string;
